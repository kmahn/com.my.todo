import { AuthTokensRepositoryImpl } from './auth-tokens-repository.impl';

describe('AuthTokensRepositoryImpl', () => {
  it('should be defined', () => {
    expect(new AuthTokensRepositoryImpl()).toBeDefined();
  });
});
